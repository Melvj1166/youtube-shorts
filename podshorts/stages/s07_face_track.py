from podshorts.types import Context, FaceTrackInput, FaceTrackOutput


def run(input: FaceTrackInput, ctx: Context) -> FaceTrackOutput:
    # TODO: Step 6 — MediaPipe Face Detection, Kalman filter smoothing (filterpy),
    # process only frames within selected segment ranges.
    raise NotImplementedError("s07_face_track not yet implemented")
